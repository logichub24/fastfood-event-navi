# AGENTS.md — 개발 공통 지침

이 저장소에서 작업하는 사람과 에이전트가 따르는 규칙입니다.
**이 앱은 출시되어 운영 중입니다. 기능 추가보다 기존 정상 동작 보존이 우선입니다.**

---

## 0. 이 프로젝트에서 특히 조심할 것

작업 전 [PROJECT_SPEC.md](PROJECT_SPEC.md)를 먼저 읽으세요. 요약하면 이렇습니다.

- **데이터 수집 경로가 두 개이고 서로를 보완합니다.** 서브웨이·맘스터치는 로컬 PC에서만,
  롯데리아는 GitHub Actions에서만 수집됩니다. `runAll.js`의 폴백이 상대가 넣은 값을 지킵니다.
  **한쪽만 남기지 마세요.**
- **데이터는 앱 재배포 없이 반영됩니다.** `deals.json`/`stores.json`은 번들에 없고 런타임에 받습니다.
  앱 코드(`public/*`)를 고칠 때만 `ait build` → `ait deploy`가 필요합니다.
- **`git push`는 앱 배포가 아닙니다.** `dist/`와 `*.ait`는 gitignore 대상입니다.
- **번들러가 없습니다.** `public/`의 파일이 그대로 배포됩니다. `type="module"`을 쓰면
  인라인 `onclick` 핸들러가 전역 함수를 못 찾아 전부 깨집니다.
- 결정 배경과 과거에 밟은 지뢰는 [context-notes.md](context-notes.md)에 있습니다.
  같은 실수를 반복하지 않도록 작업 전에 훑어보세요.

---

## 1. 기본 개발 원칙

1. 기존에 정상 작동하는 기능을 최우선으로 보존한다.
2. 코드를 수정하기 전에 프로젝트 구조와 현재 구현 상태를 먼저 파악한다.
3. 요청하지 않은 기능이나 화면을 임의로 대규모 변경하지 않는다.
4. 문제 해결에 필요한 최소 범위만 수정한다.
5. 한 문제를 해결하면서 다른 정상 기능을 손상시키지 않는다.
6. 기존 코드를 무조건 새 구조로 재작성하지 않는다.
7. 대규모 리팩터링은 명확한 필요성이 있을 때만 한다.
8. 수정 후 반드시 빌드와 관련 기능을 검증한다.
9. **검증하지 않은 내용을 정상 작동한다고 판단하지 않는다.**
10. 임시방편보다 유지보수 가능한 해결책을 우선한다.

## 2. 작업 시작 전 파악할 것

디렉터리 구조, `package.json`, `AGENTS.md`, `PROJECT_SPEC.md`, `README.md`, Git 상태와 브랜치,
최근 변경, 프레임워크/라이브러리, Toss SDK 버전, API·서버 구조, 환경변수, 빌드 명령,
배포 구조, 주요 화면과 라우팅, 기존 테스트, GitHub Actions 유무.

## 3. 작업 범위 관리

`요청 확인 → 관련 코드 탐색 → 원인 파악 → 최소 수정 → 검증`

"검색 개선" 요청에 홈·광고·로그인·디자인까지 함께 손대지 않습니다.
자동 포맷팅으로 프로젝트 전체 파일이 바뀌는 것도 피합니다.

## 4. 기존 정상 기능 보호

다음은 특히 신중하게 다룹니다.

Toss SDK 초기화 / 앱 진입 / 라우팅 / 뒤로가기 / 외부 링크 / 광고 / API 인증 /
서버 통신 / 데이터 저장 / 결제·구매 이동 / ShareLink / 환경변수 / 배포 설정 / GitHub Actions

변경이 필요하면 기존 동작을 먼저 파악하고, 변경 후 회귀를 확인합니다.

## 5. 신규 기능 개발 순서

`목적 → 데이터 → 로직 → UI → 예외처리 → 검증`

UI부터 만들고 데이터 구조를 억지로 맞추지 않습니다.
구현 가능성이 확인되지 않은 기능을 완성된 것처럼 UI에 먼저 넣지 않습니다.

## 6. 단순성 우선

`핵심 기능 완성도 > 사용 편의성 > 안정성 > 부가 기능 수`

기능이 많다고 좋은 앱이 아닙니다. 한 화면에 너무 많은 기능을 넣지 않습니다.

## 7. 모바일 우선

작은 화면, 긴 화면, Safe Area, 하단 영역, 키보드, 긴 텍스트, 스크롤, 터치 영역,
모달, 바텀시트, 시스템 UI 충돌을 확인합니다.
**PC 브라우저에서 정상이라고 모바일에서도 정상이라고 가정하지 않습니다.**

## 8. Toss Mini App 원칙

앱 시작 / SDK 초기화 / Safe Area / 뒤로가기 / 하단 탭 / 외부 이동 / Toss 복귀 /
WebView / 바텀시트 / 모달 / 광고 / 외부 브라우저 / 실제 Android 기기를 확인합니다.
정책이나 SDK 동작이 불확실하면 추측하지 말고 공식 문서를 기준으로 확인합니다.

## 9. 뒤로가기

`홈 → 상세 → 뒤로 → 홈`, `앱 → 외부 → 뒤로 → 앱`, `앱 → Toss 내부 → 뒤로 → 앱`
각각 확인합니다. 뒤로가기 한 번으로 앱이 죽거나 엉뚱한 화면으로 가지 않아야 합니다.
브라우저 테스트만으로 판단하지 않습니다.

## 10. 화면 상태

데이터를 쓰는 화면은 **Loading / Success / Empty / Error** 넷을 모두 고려합니다.
로딩 중 빈 화면을 보여주지 않고, Empty를 오류처럼 보이게 하지 않으며,
Error에는 다시 시도할 방법을 제공합니다.

## 11~16. 데이터와 API

- 외부 API는 공식 사양(엔드포인트·인증·파라미터·응답·호출 제한·갱신 주기·오류 코드)을 먼저 확인합니다.
- **API Key·Secret을 코드에 직접 쓰지 않습니다.** 환경변수나 서버 Secret으로 관리하고
  GitHub에는 `.env.example`만 둡니다.
- Secret이 필요한 외부 API는 `Mini App → 자체 서버 → 외부 API` 구조를 씁니다.
- 같은 데이터를 화면 이동마다 반복 요청하지 않고 캐시를 씁니다.
- 외부 API 장애가 앱 전체 장애가 되지 않게 `최신 → 캐시 → 오류 안내` 순으로 처리하고,
  Timeout을 두며 재시도를 무제한으로 하지 않습니다.
- 외부 데이터가 항상 정상이라고 가정하지 않습니다. `null`, 빈 문자열, 필드 누락, 잘못된 숫자,
  이미지 없음, 중복, 예상 못한 카테고리, 잘못된 URL, 응답 구조 변경을 고려합니다.
  **하나의 잘못된 데이터로 전체 화면이 안 그려지는 구조를 피합니다.**

## 17. TypeScript

`any`를 최소화하고 API 응답에 명확한 타입을 정의합니다.
타입 오류를 억지로 무시해 빌드를 통과시키지 않습니다.
*(이 프로젝트는 순수 JS라 현재 해당 없음)*

## 18~19. 오류 처리와 로깅

네트워크·Timeout·잘못된 응답·이미지 실패·4xx·5xx·입력 오류를 처리합니다.
오류를 숨기지 말고 원인을 확인할 로그를 남깁니다.
단 **Secret·비밀번호·토큰·불필요한 개인정보는 로그에 남기지 않습니다.**

## 20~21. UI/UX와 일관성

장식보다 정보 전달과 사용성을 우선합니다. 중요한 정보가 먼저 보이고,
같은 의미의 영역을 반복하지 않으며, 강조 요소를 남용하지 않습니다.
버튼은 누르면 무엇이 일어날지 예상되어야 합니다.
버튼·카드·라운드·여백·글자 크기·배지·칩·모달·시트·로딩·빈 상태·오류 상태를 앱 안에서 통일합니다.

## 22~23. 아이콘과 썸네일

- 아이콘: 1:1, 1024×1024 권장, 배경이 캔버스를 꽉 채움, 투명·빈 모서리 금지,
  심볼 크롭 금지, 안전여백 확보, 작은 크기에서도 식별 가능, 글자 남용 금지
- 썸네일: 1932×828, 전체 배경 적용, 주요 오브젝트 크롭 금지, 상하좌우 안전여백,
  핵심 기능이 즉시 이해되어야 함

## 24. 광고

사용자 경험을 심각하게 방해하지 않는 위치에 둡니다. 많이 노출한다고 수익이 좋아지지 않습니다.
상세 확인·결과 확인·길찾기·혜택 사용·추가 정보 열람 같은 시점을 우선 검토합니다.
광고 종료 후 원래 화면으로 정상 복귀해야 하고, 중복 호출을 막으며,
**광고 실패가 핵심 기능을 중단시키지 않아야 합니다.**

## 25. Analytics

방문자 수만으로 성과를 판단하지 않도록 주요 행동 이벤트를 남깁니다.
`app_open` `home_view` `search` `item_click` `category_click` `favorite` `share`
`external_link` `ad_impression` `error` 등. 이벤트 이름은 앱 안에서 일관되게 유지합니다.

## 26~28. Git과 CI

- 변경 단위를 작게 유지합니다. `feat` `fix` `ui` `api` `chore` `release`
- 대규모 변경 전에 복구 가능한 커밋·브랜치가 있는지 확인합니다.
- Push 전 확인: Secret 없음 / `.env` 제외 / 대용량 파일 없음 / 빌드 성공 / 주요 오류 없음
- `main`은 가능한 정상 작동·출시 가능 상태를 유지합니다.
- CI로 `Install → Type Check → Lint → Test → Build`를 자동화합니다.
  **CI를 통과시키려고 검사를 제거하거나 오류를 숨기지 않습니다.**

## 29. 자동 수정 안전 원칙

- 자동 수정 가능: 명확한 빌드 오류, 단순 타입 오류, 의존성 문제, 반복 확인된 CI 문제
- 주의: 데이터 구조·API 계약 변경, 인증, 결제, 광고 정책, Toss SDK 핵심 설정,
  DB 마이그레이션, 대규모 리팩터링
- **위험도가 높으면 자동 수정보다 원인 보고를 우선합니다.**

## 30~31. 파생 앱과 독립성

운영 중인 원본을 다른 앱으로 바꾸지 않습니다. 복제 후 새 앱으로 설정합니다.
기술 기반(SDK·API 클라이언트·공통 컴포넌트·오류 처리·광고·계측)은 재사용하되
앱 이름·appId·도메인·아이콘·썸네일·핵심 목적·홈 화면·색상·카피는 새로 정의합니다.
파생 후 원본 이름이나 설정이 남아 있는지 검색합니다.

## 32~33. 성능과 이미지

초기 번들 크기, 이미지 크기, API 호출 횟수, 불필요한 리렌더, 대용량 목록,
Lazy Loading, 캐시, 초기 로딩 속도를 고려합니다.
단순한 기능에 무거운 라이브러리를 추가하지 않습니다.
원본보다 지나치게 큰 이미지를 CSS로만 축소해 쓰지 않습니다.

## 34~37. 저작권·개인정보·위치·접근성

- 인터넷 이미지를 자유롭게 쓸 수 있다고 가정하지 않습니다. 권리가 불분명하면
  자체 제작·라이선스 명확한 이미지·텍스트 중심 UI를 씁니다.
- 기능에 필요하지 않은 개인정보를 수집하지 않습니다.
- 위치 권한을 거부해도 기본 기능을 제공하고, 위치 실패로 앱이 멈추지 않게 합니다.
- 글자 크기·대비·터치 영역을 확보하고, 색상만으로 상태를 표현하지 않으며,
  의미 있는 버튼명과 이미지 대체 정보를 제공합니다.

## 38~39. 의존성과 삭제

새 라이브러리는 정말 필요한지, 기존 것으로 가능한지, 유지보수되는지,
번들을 키우지 않는지, 라이선스 문제가 없는지 확인합니다.
**사용하지 않아 보이는 코드도 바로 지우지 않습니다.** 다른 화면·동적 import·
서버 작업·빌드/배포 스크립트·Toss 설정·GitHub Actions에서 쓰는지 먼저 검색합니다.

## 40~41. 검증

`Type Check → Lint → Test → Production Build` 순으로 확인합니다.
**빌드 성공만으로 완료라고 판단하지 않습니다.**
`홈 → 검색 → 상세 → 외부 이동 → 뒤로가기`처럼 실제 사용자 행동 순서로 확인합니다.

## 42~43. 보고

완료 시 **변경 내용 / 변경 파일 / 검증 / 남은 문제 / 위험요소**를 정리합니다.
빌드 실패, 테스트 실패, API 확인 불가, 실기기 확인 필요, 외부 서비스 확인 불가,
정책 확인 필요인 경우 **완료라고 보고하지 않습니다.**
확인하지 못한 것은 확인하지 못했다고 명확히 구분합니다.

## 44. 출시 전 검사

프로덕션 빌드 성공 / 치명적 타입 오류 없음 / 주요 기능·API 정상 /
Loading·Empty·Error 정상 / 뒤로가기 정상 / 외부 링크·Toss 복귀 정상 / 광고 정상 /
Safe Area 정상 / 하단 잘림 없음 / Secret 노출 없음 / 아이콘·썸네일 규격 정상 /
개인정보·저작권 문제 없음 / 제휴·광고 고지 적용 / GitHub 최신 / 복구 가능한 버전 존재

## 45~46. 운영과 유지보수

출시는 끝이 아니라 운영의 시작입니다. 데이터 업데이트, API 장애, SDK 업데이트,
정책 변경, 서버 장애, 도메인 변경, 사용자 증가, 광고 변경, 기능 폐기, 복구, PC 교체를 고려합니다.
**특정 PC에서만 실행 가능한 구조를 만들지 않습니다.**
몇 달 뒤 다시 열어도 구조를 이해할 수 있도록 `AGENTS.md` `PROJECT_SPEC.md` `README.md`
`.env.example`과 변경 기록을 최신으로 유지합니다.

## 47. 우선순위

충돌하면 이 순서를 따릅니다.

1. 데이터 및 사용자 보안
2. 기존 정상 기능 보호
3. 앱 안정성
4. 플랫폼 정책 준수
5. 핵심 기능 정확성
6. 사용 편의성
7. 성능
8. 유지보수성
9. 디자인 개선
10. 부가 기능 추가

디자인 개선이나 새 기능 때문에 1~5를 희생하지 않습니다.

## 48. 에이전트 행동 원칙

작업 중 명확한 문제를 발견하면 알립니다. 단 발견한 모든 문제를 임의로 고치지 않습니다.

- **즉시 수정** — 현재 요청과 직접 관련되고 안전한 문제
- **보고 후 판단** — 요청과 무관하거나 영향 범위가 큰 문제
- **수정 금지** — 데이터 손실, 운영 장애, Secret 노출, 예상 못한 대규모 변경 가능성

## 49~50. 과도한 개발 방지

목적에 필요하지 않은 기능을 구현 가능하다는 이유만으로 추가하지 않습니다.
거의 쓰지 않을 설정, 중복 기능, 의미 없는 메뉴 증가, 데이터가 부족한 기능,
재방문 가치 없는 장식, 유지보수 비용만 늘리는 기능을 경계합니다.

> 빠르게 많이 수정하는 것보다 **정상 작동하는 앱을 안전하게 개선**합니다.
> 개발 완료의 기준은 코드 작성 완료가 아니라 **검증 가능한 정상 작동**입니다.
