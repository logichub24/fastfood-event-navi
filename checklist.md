# 작업 체크리스트 — 4개 항목 (KFC 문서정정 / 광고 튜닝 / 재방문 동기 / 파일 분리)

이전 Lottie 체크리스트는 폐기됨(context-notes.md 참고). 새 범위로 교체.

## 1. KFC 매장 위치 "미지원" 표기 정정
실제로는 카카오 로컬 API로 186건 수집 중. 코드가 아니라 주석·문서만 낡음.

- [x] `scripts/storeLocations.js` 헤더 주석에서 KFC 미지원 서술 수정
- [x] `.github/workflows/crawl.yml` 스텝 이름에서 "KFC는 아직 미지원" 제거
- [x] `public/index.html` EVENT_BRANDS 주석 수정 (버거킹/노브랜드버거 크롤러는 실재함)

## 2. 광고 트리거 튜닝 (수익화 ↔ 경험 균형)
- [x] 앱 시작 후 유예 45초
- [x] 전역 쿨다운 90초
- [x] 세션당 노출 상한 5회
- [x] navigation 빈도 매번 → 2회당 1회
- [x] 일반 브라우저에서 조용히 no-op 유지 확인 (콘솔 에러 0)

## 3. 재방문 동기 — "지난 방문 이후 새 행사"
- [x] 마지막 방문 시점의 행사 키 집합을 localStorage에 저장
- [x] 저장본과 비교해 개인화 신규 산출
- [x] 배너 추가 + 탭하면 해당 행사만 필터
- [x] 첫 방문자는 배너 미노출
- [x] 저장 실패·로드 실패에도 앱이 깨지지 않도록 방어

## 4. index.html 분리 (1547줄 → 299줄)
- [x] `<style>` → `public/app.css` (144줄)
- [x] 인라인 `<script>` → `public/app.js` (1168줄)
- [x] `scripts/build-toss.js` 복사 목록 갱신
- [x] `public/sw.js` PRECACHE 갱신 + 캐시 버전 v1→v2
- [x] onclick 인라인 핸들러 유지 위해 `type="module"` 미사용

## 검증
- [x] 행사 목록 74건 렌더 / 상세 시트 / 검색 39건 / 찜 토글
- [x] 지도 타일·마커 (매장 5,243건, 반경 내 마커 7개) — rAF 우회 직접 호출로 확인
- [x] 콘솔 에러 0
- [x] `node scripts/build-toss.js` 성공, dist에 app.css·app.js 포함 확인
- [x] 항목별 커밋 분리
- [ ] 실기기(토스 인앱)에서 광고 가드 실제 동작 확인 — 프리뷰에서는 SDK가 no-op이라 미검증

---

# 계측 도입 (출시 후 DAU 30~50)

목표: "매일 오는 30~50명이 같은 사람인가"에 답할 근거를 만든다. 답이 나오기 전엔 기능을 더 얹지 않는다.

- [x] 토스 자체 `@apps-in-toss/web-analytics` 사용 (새 의존성 없음)
- [x] 계측 실패가 앱을 깨지 않도록 동적 import로 격리 + `window.tossLog` 가드
- [x] `ff_last_visit` 저장 → 신규/재방문·방문 간격 계산
- [x] `screen: app_open` (visit_type·days_since_last_visit·liked_count·since_last_visit_new)
- [x] `since_visit_banner` 노출/클릭 짝으로 계측 (노출은 방문당 1회)
- [x] `deal_detail` / `deal_like` / `deal_share` / `tab_map` / `store_navigate` 클릭
- [x] 스텁으로 첫 방문·3일 만의 재방문 시뮬레이션 검증, 콘솔 에러 0
- [x] **토스 콘솔에서 기존 지표 먼저 확인** — AU/신규는 콘솔이 제공. 내 계측은 콘솔이 모르는
      "앱 안 행동 × 신규/재방문"을 담당하므로 중복 아님. 기준선은 context-notes 참고.
- [ ] 실기기에서 이벤트가 실제 콘솔에 적재되는지 확인
- [ ] 1~2주 지표 관찰 후 다음 작업 결정 (그 전엔 기능 추가 보류)

---

# 첫 세션 개선 (2026-08-02, 콘솔 전체 지표 기반)

근거: 검색 유입 90% / 체류 34초 / D1 리텐션 1.59% / 첫 방문자 91%.

- [x] 1. 면책 모달 → 목록 위 한 줄 안내 (전문은 '자세히'·설정에서 유지)
- [x] 2. 브랜드 칩을 행사 수 내림차순 + 건수 표시 (버거킹 20건이 숨던 문제)
- [x] 3. 검색 의도 이어받기 (q/query/keyword/kw/search/term + 해시, 방어적)
- [x] 4. 첫 상세 열람 시 찜 힌트 1회 (비차단 토스트)
- [x] 5. 알림 템플릿 미등록 시 SDK 호출 차단 + 안내 메시지
- [x] 검증: 첫 방문 플로우 / 안내 닫기 / 검색어 6종 예외 / 전체 회귀 / 콘솔 에러 0
- [ ] **토스 콘솔에서 알림 발송 코드 등록** → `ads.js`의 `NOTIFICATION_TEMPLATE_CODE` 교체
- [ ] **실기기에서 검색 유입 시 URL 파라미터 확인** → 3번이 실제로 동작하는지 판정
- [ ] **미니앱홈 노출 문의** (유입 0.4%, 코드 아님)
- [ ] 배포 (`ait build` → `ait deploy`)

---

# 레이아웃 교체 + 스크롤 깊이 계측 (2026-08-02)

- [x] 카테고리 칩 줄 → 브랜드 2번째 줄 (헤더 높이 167px 유지, 브랜드 노출 3개→6개)
- [x] 카테고리는 버튼 하나로 축소하되 선택값을 라벨·색·aria-label에 표시 (정보 손실 방지)
- [x] 칩 넘침 방지 가로 스크롤 (건수 표시로 라벨이 길어져 버튼을 덮던 버그)
- [x] 스크롤 깊이 계측 — 구간을 이벤트 이름에 넣어 파라미터 분포 없이도 읽히게
- [x] rootMargin(120px)이 화면 밖 카드를 집계해 깊이가 부풀던 문제 수정
- [x] 구간 재설정 (스크롤 없이 3장이 보이므로 1_3 / 4_8 / 9_20 / 21up)
- [x] 검증: 세션당 최대 4건·중복 없음, 스크롤 안 하면 1_3만, 전체 회귀·콘솔 에러 0
- [ ] **토스 콘솔이 이벤트 파라미터를 쪼개 보여주는지 확인** — 안 되면 기존 계측도 이름 기반으로 정리 필요
- [ ] 카드 높이 축소는 **보류**. 스크롤 깊이 데이터를 보고 결정한다.

---

# 광고 시점 조정 (2026-08-03)

- [x] 시작 유예 45초 → 20초 (실측 체류 34초와 충돌해 전면광고가 아예 안 뜨던 문제)
- [x] 쿨다운 90초·세션 상한 5회는 유지 (안전장치, 비용 없음)
- [x] 노출 계측 추가 — `interstitial_<트리거>` + 경과 초·세션 내 순번
- [x] 토스 앱 밖에서 트리거 18회에도 광고 이벤트 0건 (조용히 no-op 유지)
- [ ] **실기기에서 유예·쿨다운이 의도대로 걸리는지 확인** — 프리뷰에서는 SDK가 no-op이라 불가

---

# 자체 앱 교차 홍보 (2026-08-03)

- [x] 목록 맨 아래 + 빈 결과 화면에 두 앱 나란히 노출 (상단 첫 줄은 첫 세션 손해라 반대)
- [x] `intoss://cvs-event-navi` / `intoss://cafe-event-map` 딥링크
- [x] 앱별 클릭 계측 + `max_card`(얼마나 스크롤한 사람이 눌렀는지)
- [x] 접근성 aria-label, 이미지 로드 실패 시 텍스트만 유지
- [x] 검증: 첫 화면 미노출, 지연 로드, 빈 결과에서도 노출, 콘솔 에러 0
- [ ] **아이콘 용량** — 각 290~311KB(600×600 원본). 토스 CDN이 리사이즈 미지원.
      작은 아이콘을 CDN에 새로 올리면 해결. 지금은 lazy라 끝까지 스크롤한 사람만 받는다.
- [x] **토스 심사에서 미니앱 상호 홍보 허용 여부** — 확인됨. 편의점 행사 앱에서 이미 운영 중이라
      심사 전례가 있다. 다음 세션에서 이 걱정을 다시 꺼내지 말 것.

---

# 상단 회전 칩 + 헤더 정리 (2026-08-03)

- [x] 상단 첫 줄에 자체 앱 회전 칩 (편의점 앱과 같은 형태, 4초 주기)
- [x] 오탭 방지 — 손가락이 닿으면 회전 정지
- [x] 노출은 앱당 1회만 기록, 백그라운드에서는 회전 건너뜀
- [x] 계측 분리 — `cross_promo_top_*` / `cross_promo_bottom_*`
- [x] 첫 줄 넘침 해결 (28px → 0px): 간격·패딩 축소 + 홍보 칩 1줄화
- [x] 설정 버튼을 하단 네비로 이동 → 스크롤 영역 250px → 290px
- [x] app.js의 배너 className 문자열도 동기화 (안 하면 렌더 직후 원복됨)
- [x] 검증: 회전·href·aria 동기화, 오탭 방지, 설정 시트, 탭 전환, 전체 회귀, 콘솔 에러 0
- [ ] 4칩(오늘 종료까지) 상황에서 홍보 칩이 밀리는 건 의도적으로 허용 — 필요 시 순서 조정

---

# ~~다음 세션 — 배포 후 일주일 (데이터 확인)~~ 폐기

계측(tossLog)이 브릿지 SDK 이중 로드로 광고를 죽여서 **출시 전에 제거됐다.**
아래 항목들은 앱 내부 행동 데이터를 전제로 해서 전부 실행 불가능하다. 아래 새 계획으로 대체.

---

# 2026-08-11 — 파이프라인 복구 + 로컬 크롤 자동화

- [x] 매장 API 타임아웃이 행사 데이터 갱신까지 막던 문제 수정 (continue-on-error)
- [x] 소상공인 API 재시도 3회 + 타임아웃 45초
- [x] 푸시 경합 시 리베이스 후 재시도, 커밋 날짜 KST로
- [x] 서브웨이·맘스터치 3주 만에 복구 (로컬 실행, 서브웨이 6건 / 맘스터치 4건)
- [x] `scripts/daily-crawl.js` 작성 (20시 / 실패 시 21시 / 중복 실행 방지)
- [x] Windows 예약 작업 2개 등록 + 강제 실행으로 결과 코드 0 확인
- [x] 데이터가 앱 재배포 없이 라이브 반영되는 것 확인

---

# 다음 세션 (2026-08-18 무렵) — 출시 후 지표 분석

**사용자가 다음 주로 미룸.** 8/11에는 파이프라인 장애 대응이 우선이었다.

## 시작 전에 필요한 것 (사용자만 줄 수 있음)
- [ ] 토스 콘솔 CSV 5종 — AU / 리텐션 / 체류 시간 / 유입 경로 / 연령
      기간은 **출시 전 2주가 포함되도록** 넉넉히
- [ ] **실제 출시 반영 날짜** — 빌드는 8/03이지만 업로드·심사 반영일은 미확인.
      이 날짜로 전후를 잘라야 비교가 된다.

## 비교 기준선 (출시 전 실측, 2026-07-05~08-01)
| 지표 | 값 |
|---|---|
| 일평균 AU | 47.2 |
| 체류 시간 | 34.0초 |
| D1 리텐션 | 1.59% |
| D7 리텐션 | 0.76% |
| 재방문 비중 | 8.5% |
| 검색 유입 | 89.9% |

## 무엇을 볼 것인가
이번 릴리스는 **첫 세션**을 겨냥했다(면책 모달 제거, 브랜드 3→6개, 설정 하단 이동).
따라서 **체류 시간과 D1 리텐션**이 먼저 움직였을 지표다.

⚠️ 앱 내부 행동 데이터는 없다. 콘솔 지표만으로 판단해야 한다.
⚠️ 하루 47명 규모라 일주일 표본은 300명대다. 코호트별 D1은 특히 흔들린다.
방향성 정도로 읽고, 확신이 필요하면 2~3주를 볼 것.

## 그 사이 지켜볼 것
- [ ] 예약 작업이 실제로 매일 도는지 (`daily-crawl.log`, UTF-8이라 `-Encoding utf8` 필요)
- [ ] 크롤 워크플로우가 다시 실패하지 않는지
- [ ] PC가 꺼져 빠지는 날이 잦으면 국내 VPS 검토 (월 5,000~10,000원. 다만 차단 기준이
      지역인지 데이터센터인지 몰라 국내 클라우드도 막힐 수 있다 — 쓰기 전엔 알 수 없다)
