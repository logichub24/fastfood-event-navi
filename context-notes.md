# 컨텍스트 노트

---

# 2026-08-03 — 상단 회전 칩 추가 + 설정을 하단 네비로 이동

## 배치 결정 번복
앞서 상단 배치를 반대했으나, 사용자가 편의점 행사 앱의 적용 캡처를 보여줬다.
실제 형태는 **배너가 아니라 다른 칩과 같은 크기의 칩**이라 부담이 작았고, 사용자 결정에 따라
**상단 회전 칩 + 하단 안내를 둘 다** 두기로 했다.
두 자리는 서로 다른 순간을 잡는다 — 상단은 진입 직후, 하단은 다 보고 난 뒤.
계측이 `cross_promo_top_*` / `cross_promo_bottom_*`로 분리돼 있어 **어느 자리가 실제로
유입을 만드는지 데이터로 판가름난다.** 효과 없는 쪽은 다음 릴리스에서 뺀다.

캡처로 앱 ID도 교차 검증했다 — `cafe-event-map`, `cvs-event-navi`(끝에 i), 아이콘 URL 2개 모두 일치.

## 회전 칩 구현
- 4초 주기로 두 앱을 번갈아 표시. `href`/`aria-label`/아이콘/이름을 매 회전마다 함께 갱신한다.
- **오탭 방지**: `pointerdown`에서 회전을 멈춘다. 누르려는 순간 앱이 바뀌면 엉뚱한 앱이 열린다.
  클릭 시에도 한 번 더 멈춘다.
- 백그라운드에서는 `visibilityState`를 보고 회전을 건너뛴다.
- **노출은 앱당 1회만** 기록(`promoSeen`). 회전할 때마다 쌓으면 클릭률이 무의미해진다.
- 앱이 1개뿐이면 타이머를 아예 걸지 않는다.

## 상단 첫 줄 공간 문제와 해결
칩을 추가하니 첫 줄이 **28px 넘쳐 홍보 칩이 검색 버튼 위로 잘려** 보였다(사용자가 캡처로 지적).
- 1차: 간격·패딩 축소(`gap-1.5`→`gap-1`, 칩 패딩 축소, 홍보 칩을 2줄→1줄 아이콘+이름+화살표)
  → 넘침 28px → 3px → 0px. **터치 영역 확보를 위해 아이콘 버튼 크기(w-9)는 건드리지 않았다.**
- 2차(사용자 제안): **설정 버튼을 하단 네비로 이동.** 자주 쓰지 않는데 상단 폭을 차지하고 있었다.
  → 스크롤 영역이 250px → **290px**로 늘어 여유가 생겼다.
- ⚠️ `updateAlertBanners`가 배너 className을 통째로 덮어쓰므로 **app.js의 클래스 문자열도 함께
  고쳐야 한다.** HTML만 고치면 렌더 직후 원래 간격으로 되돌아간다.

## 버그 — 아이콘이 계속 안 보이던 원인 (`src=""`)
설정을 하단으로 내려 여유가 생긴 뒤 칩을 편의점 앱과 같은 형태(아이콘 26px + 이름 + '바로가기')로
복원했는데 **아이콘이 렌더되지 않았다.** 원인은 HTML의 `src=""`다.
빈 값도 로드 실패로 처리돼 `onerror`가 즉시 발동, `display:none`이 박힌다.
그 뒤 JS가 진짜 URL을 넣어도 이미지는 정상 로드되지만 **display는 none으로 남는다**
(로그상 `complete:true, naturalWidth:600`인데 `렌더폭:0`이었다).
→ HTML에서 `src` 속성을 아예 빼고, `paintPromoChip`에서 src를 넣을 때 `display`를 초기화한다.
**교훈: onerror가 달린 img에 빈 src를 두지 말 것.**

## 회전 검증에서 헛짚은 것
회전 주기(4초)와 거의 같은 간격(4.3초)으로 두 번 샘플링해 "두 번째 회전이 안 된다"고 의심했으나,
1초 간격 12회 샘플링으로 보니 4초마다 정상 전환됐다. **주기와 비슷한 간격으로 샘플링하면
안 바뀐 것처럼 보인다.** 주기성 검증은 주기보다 훨씬 짧은 간격으로 샘플링할 것.

## 남은 한계
'오늘 종료' 칩까지 함께 뜨는 날은 4칩이 되어 **60px 넘치고 홍보 칩이 뒤로 밀린다.**
가로 스크롤로 접근은 되고 하단 안내도 있으므로 그대로 뒀다.
앱 자체 필터가 외부 홍보보다 우선이라는 판단.

---

# 2026-08-03 — 자체 앱 교차 홍보 (편의점 행사 / 카페행사맵)

## 위치 결정 — 상단 첫 줄 제안을 목록 맨 아래로 바꿨다
사용자 제안은 **상단 첫 줄 '새 행사' 옆에 아이콘을 회전 노출**하는 것이었다. 반대한 이유는 셋이다.
- 상단 첫 줄은 34초 세션에서 가장 비싼 자리이고 방문자의 **91%가 첫 방문자**다.
  거기에 "다른 앱으로 나가세요"를 두는 건 첫 세션 마찰을 줄여온 작업과 정반대다.
- 교체되며 움직이는 아이콘은 콘텐츠가 아니라 **가장 좋은 자리에 놓인 광고**다.
  D1 리텐션 1.59%인 상태에서 사용자를 내보내는 장치를 최우선 자리에 둘 이유가 없다.
- 회전 방식은 어느 앱이 노출됐을 때 눌렸는지 계측이 복잡해지고, 사용자가 원하는 앱을 기다려야 한다.

→ **목록 맨 아래(`#crossPromo`)에 두 앱을 나란히 정적으로** 배치했다.
목록을 끝까지 본 사람 = 여기서 볼 게 없어진 사람이라 자연스러운 퇴장 지점이고,
빈 결과 화면("조건에 맞는 행사가 없어요") 바로 아래에도 같은 요소가 노출된다.
요소 하나로 두 상황을 모두 커버한다(빈 결과일 때 목록이 비므로 자연히 그 아래에 붙는다).

## 구현
- `OTHER_APPS`에 앱 ID·아이콘·설명. 딥링크는 `intoss://<앱ID>`(공유 링크가 이미 쓰던 스킴).
- 이동은 `<a href>`가 처리하므로 토스 앱 밖에서는 자연히 아무 일도 일어나지 않는다.
- 계측은 앱별로 이름을 나눴다 — `cross_promo_cvs_event_navi` / `cross_promo_cafe_event_map`.
  `max_card`를 함께 보내 **얼마나 스크롤한 사람이 눌렀는지**까지 본다.
- 접근성: `aria-label`("… 앱 열기"), 이미지 `alt=""`(장식) + 텍스트 라벨 병기.

## 남은 문제 — 아이콘 용량
아이콘이 **600×600 원본, 각 290~311KB**다. 36px로 표시하는데 과하다.
`static.toss.im`은 리사이즈 파라미터(`?w=`, `?width=`, `?s=`)를 **지원하지 않는다**(전부 원본 반환).
- 지금은 `loading="lazy"` + 화면 밖 배치라 **끝까지 스크롤한 사람만 내려받는다.** 첫 화면 렌더에 영향 없음.
- 브라우저 캔버스로 72×72(약 12KB)로 줄이는 것까지는 성공했으나, base64를 셸로 통과시켜
  파일로 저장하는 과정이 깨지기 쉬워 보류했다.
- **더 나은 해결책은 작은 아이콘을 토스 CDN에 새로 올리는 것**(사용자 작업).
  스크롤 깊이 데이터에서 끝까지 오는 비율이 높게 나오면 그때 처리할 것.

## 확인 못 한 것
**토스가 미니앱 간 상호 홍보를 심사에서 허용하는지 모른다.** 자기 소유 앱끼리라 문제없을
가능성이 높지만, 배포 후 반려되면 릴리스 전체가 밀린다. 콘솔 정책이나 담당자 확인 필요.

---

# 2026-08-03 — 광고 유예 45초 → 20초 (체류 34초와 충돌하던 문제)

## 무엇이 잘못됐나
전면광고 가드를 넣을 때 시작 유예를 45초로 뒀는데, **그 시점엔 체류 시간 데이터를 보기 전이었다.**
이후 콘솔 실측이 **평균 체류 34.0초**(28일 중 최장 일평균도 48.5초)로 나왔다.
→ 대부분의 세션이 유예 안에 끝나 **전면광고가 한 번도 뜨지 않는 상태**였다. 배너만 돌고 있었다.

가드 3개 중 실제로 걸리는 건 유예뿐이었다. 쿨다운 90초·세션 상한 5회는 34초 세션에서
애초에 도달하지 않는다. 즉 유일하게 작동하는 가드가 광고를 꺼버리는 방향으로 작동했다.

## 조정
- `AD_START_GRACE_MS` 45000 → **20000**.
  20초면 첫 화면을 보고 카드를 한 번 눌러본 뒤라 "가치를 먼저 보여준다"는 취지는 유지된다.
  트리거(지도 진입·길찾기·상세)가 이미 사용자의 목적 행동이라 불쑥 튀어나오는 구조가 아니다.
- 쿨다운·세션 상한은 **그대로 유지**. 지금은 거의 안 걸리지만 오래 머무는 소수에게
  광고가 연달아 뜨는 최악의 경우를 막는 비용 없는 안전장치다.

## 계측 추가
조정 효과를 확인하려면 "실제로 떴는가"가 남아야 해서 노출 시 이벤트를 남긴다.
`interstitial_map` / `interstitial_navigation` / `interstitial_detail` — **트리거를 이름에 넣어**
콘솔이 파라미터 분포를 못 보여줘도 어느 시점에서 떴는지 읽히게 했다(다른 계측과 같은 방침).
`seconds_since_start`, `nth_in_session`도 파라미터로 함께 보낸다.

## 규모에 대한 솔직한 기대치
하루 47명 기준이라 전면광고를 살려도 **월 수천 원대**다. 이 변경의 값어치는 수익이 아니라
**광고가 실제로 어떻게 작동하는지 데이터를 얻는 것**이다. 지금처럼 0에 수렴하면 나중에
사용자가 늘었을 때 적정 빈도를 판단할 근거가 없다.

## 검증 한계
프리뷰(토스 앱 밖)에서는 SDK가 no-op이라 `interstitialReady`가 항상 false다.
트리거를 18회 눌러도 광고 이벤트가 0건인 것 = **일반 브라우저에서 조용히 무시됨은 확인**.
하지만 유예·쿨다운이 실제로 의도대로 걸리는지는 **실기기에서만 확인 가능**하다.

---

# 2026-08-02 — 헤더 레이아웃 교체 + 스크롤 깊이 계측

## 측정으로 정리한 것 (추측으로 손대지 않기 위해)
| 항목 | 값 |
|---|---|
| 앱 높이 / 헤더 4줄 | 753px / **167px (22%)** |
| 목록 영역 | 470px |
| 카드 높이 | **188px** (74장 중 66장), 제목 2줄이면 210px (8장) |
| 카드 중 밴드(장식) | **112px = 60%** |
| 보이는 카드 | 2.5장 |

## 결정 — "브랜드 2줄" 대신 "카테고리 줄과 맞바꾸기"
- 사용자 제안은 카드 축소 + 브랜드 2줄이었다. 계산해보니 브랜드 줄 추가가 34px를 먹어
  카드 축소(188→164px)로 번 것의 절반을 반납한다(2.87장 → 2.66장).
- 그래서 **카테고리 칩 줄을 브랜드 2번째 줄로 교체**했다. 줄 수 그대로 = **높이 손해 0**,
  브랜드 노출은 3개→6개. 칩으로 닿는 행사가 46건 → **70건/74건**.
  근거는 유입 90%가 검색이고 그 의도가 대부분 브랜드라는 점.
- **카드 높이 축소는 보류.** 카드가 병목이라는 증거가 없다(스크롤 여부조차 몰랐음).
  근거 없이 줄이면 최근에 넣은 히어로 모션·브랜드 시그니처만 깎는 셈이다.

## 정보 손실 방지
카테고리 칩이 사라졌으므로 선택 중인 값이 안 보이면 안 된다.
`카테고리` 버튼 라벨을 선택값으로 바꾸고 주황 강조 + `aria-label`을 갱신한다.

## 버그 수정 — 칩이 버튼을 덮고 있었다
건수 표시로 라벨이 길어지자(`🍔 노브랜드버거 14`) 칩이 `shrink-0`인데 컨테이너에 overflow
처리가 없어 옆 모달 버튼 위로 겹쳐 렌더됐다(버튼 클릭 불가). 1행과 같이 가로 스크롤을 줘서 해결.
**교훈: 칩 라벨 길이를 바꾸면 컨테이너 overflow를 같이 확인할 것.**

## 스크롤 깊이 계측 설계
목적은 하나다 — **체류 34초가 "못 찾고 이탈"인지 "빠르게 훑고 찾음"인지 가르는 것.**
- 이미 도는 `observeCards`의 IntersectionObserver에 얹었다. 새 관측자·의존성 없음.
- **구간을 이벤트 이름에 넣었다**(`scroll_depth_1_3` 등). 토스 콘솔이 파라미터 분포를
  보여주는지 확인되지 않아서, 이름만으로 분포가 읽히게 하려는 것. ⚠️ **이 리스크는
  기존 계측 전체에 걸려 있다** — app_open의 visit_type 등도 파라미터에 실려 있다.
- **구간 도달 시 1회씩** 보낸다. 종료 이벤트(visibilitychange)에 의존하지 않아 유실이 없고,
  결과가 자연히 퍼널이 된다(1_3 = 전체, 4_8 = 스크롤한 사람…). 세션당 최대 4건.
- **함정 2개를 수정했다.**
  1. `rootMargin: 120px`이 화면 밖 카드까지 잡아 스크롤 없이도 깊이가 부풀었다.
     목록 영역 하단(`foldBottom`)과 비교해 실제 노출만 센다. rect 계산은 콜백당 1회.
  2. 스크롤 없이 3장이 보이므로 구간을 1_3 / 4_8 / 9_20 / 21up으로 다시 잘랐다.
     1_3이 "첫 화면에서 멈춤"을 뜻하게 된다.

## 데이터가 오면 이렇게 읽는다
- **1_3에 몰림** → 카드 크기 문제가 아니라 첫 화면에서 원하는 걸 못 찾는 것. 브랜드·검색을 손볼 것.
- **4_8에 몰림** → 카드가 실제 병목. 그때 카드 축소(188→164px 안)를 근거를 갖고 진행.
- **9_20·21up이 두터움** → 탐색은 잘 되는데 돌아올 이유가 없는 것. 재방문 장치(알림·찜)가 레버.

---

# 2026-08-02 — 첫 세션 개선 (콘솔 전체 지표 기반)

## 추가로 받은 실측 (앞선 기준선에 이어)
- **유입 경로: 검색 89.9%** (전체탭 8.5%, 기타 1.1%, **미니앱홈 0.4% = 28일간 6명**).
  → 토스 추천 노출이 아니라 사용자가 의도를 갖고 직접 검색해 들어온다. 유입은 문제가 아니다.
- **체류 시간 평균 34.0초** (23.6~48.5초, 07-04의 210초는 표본 극소 이상치라 제외).
- **리텐션 코호트(1,324명): D1 1.59% / D3 1.06% / D7 0.76%.** 첫 주 재방문 누적 최대 6.2%.
  → 앞서 "재방문 8.5%, 추세 개선"이라 봤던 건 **모수가 커져서 생긴 착시**에 가깝다.
    코호트로 보면 개선이 뚜렷하지 않다. 이 판단은 정정한다.

## 진단 — 34초 예산을 무엇에 쓰고 있었나
1. **첫 화면이 면책 모달이었다.** "시작하기 전에 확인해주세요" 전체 화면 모달이 뜨고
   [확인했습니다]를 눌러야 목록이 보였다. 내용은 전부 리스크 회피용이라 사용자 가치 0.
   첫 방문자가 91%라 사실상 모든 사용자가 이걸 먼저 봤다.
2. **검색 의도가 문 앞에서 버려졌다.** applyDeepLink는 `?deal=`과 map만 처리했다.
3. **브랜드 칩이 고정 순서(`EVENT_BRANDS.slice(0,3)`)였다.** 그 결과
   **행사 20건인 버거킹·14건인 노브랜드버거가 모달에 숨고, 5건인 KFC가 첫 화면을 차지**했다.

## 구현 (전부 프리뷰 검증 완료)
1. **면책 모달 → 목록 위 한 줄 안내**(`#firstNotice`). 닫으면 `ff_onboarding_seen` 저장.
   전문은 '자세히'와 설정의 '시작 안내 다시 보기'로 계속 접근 가능. 모달 자동 노출만 제거.
2. **브랜드 칩을 행사 수 내림차순 + 건수 표시.** 데이터가 매일 바뀌어도 자동으로 맞는다.
   ALL_DEALS가 비면(로드 실패) 기존 고정 3개로 폴백.
3. **검색 의도 이어받기**(`applySearchIntent`). q/query/keyword/kw/search/term + 해시를 훑고
   브랜드 별칭(맥날·쉑쉑·burger king 등)으로 매칭되면 그 브랜드를 미리 선택한다.
   - **토스가 검색어를 넘기는지는 미확인.** 그래서 안 오면 아무 일도 안 하도록 방어적으로 짰다.
     실기기에서 검색 유입 시 URL 파라미터를 확인해야 실효를 안다.
   - decodeURIComponent 실패·빈값·미매칭 모두 ALL 유지로 검증함.
4. **첫 상세 열람 시 찜 힌트 1회**(`maybeHintLike`). 찜이 0개일 때만, 토스트로. 화면을 막지 않는다.
   저장이 안 되는 환경에서는 매번 뜨는 걸 막으려고 **아예 띄우지 않는다**.
5. **알림 템플릿 가드.** `PLACEHOLDER_TEMPLATE_CODE` 상태로 SDK를 부르면 동의창이 안 뜨거나
   에러가 나므로, 호출 전에 끊고 "준비 중" 메시지를 reject한다. 콘솔 등록 후 상수만 교체하면 된다.

## 하지 못한 것 (사용자 작업 필요)
- **알림 발송 파이프라인**: 토스 콘솔 > 스마트 발송 > 기능성 탭에서 발송 코드 등록이 선행.
  이게 없으면 금·토 재방문 훅(주말 패턴 토 58 vs 화 36)도 못 만든다.
- **미니앱홈 노출**: 유입 0.4%. 콘솔·운영 영역이라 코드로 못 한다. 토스 담당자 문의 필요.
- **공유 루프**: 이미 `shareText`에 `intoss://` 딥링크가 들어 있어 추가 작업 없음.

## 중요 — 측정 교란
이번 릴리스에 첫 세션 개선 4건 + 계측 + 광고 가드가 **한꺼번에** 나간다.
사용자에게 한 번 알렸고 진행 결정을 받았다. 다음 데이터에서 **무엇이 효과였는지는 분리되지 않는다.**
전체 지표(체류·D1·재방문)가 오르면 묶음 전체의 효과로만 해석할 것.
개별 기여를 보려면 계측 이벤트(search_intent_applied, deal_detail, deal_like 등)를 봐야 한다.

---

# 2026-08-02 — 토스 콘솔 실측 기준선 (계측 시작 시점 스냅샷)

출처: 토스 콘솔 CSV 2종(AU 내역·연령 내역, 2026-07-02~08-02).
**08-02 행(au 30, new_au 0)은 집계 미완으로 보여 전 계산에서 제외.** 유효구간 07-05~08-01(28일).

## 핵심 숫자 — "매일 오는 사람이 같은 사람인가"에 대한 답
- 총 방문 1,322 user-days / 신규 1,210 / **재방문 112 = 8.5%**
- 일평균 AU 47.2, 그중 재방문은 **하루 4명**
- 즉 **밑 빠진 독이 맞다.** 한 달간 1,210명이 써보고 대부분 안 돌아왔다.

## 단, 추세는 명확한 개선 (4주 단조 증가)
| 주차 | AU | 재방문 | 비율 |
|---|---|---|---|
| 07-05~07-11 | 288 | 8 | 2.8% |
| 07-12~07-18 | 361 | 24 | 6.6% |
| 07-19~07-25 | 326 | 32 | 9.8% |
| 07-26~08-01 | 347 | 48 | **13.8%** |

총 방문은 평평한데 재방문만 5배 → 돌아오는 사람이 실제로 쌓이는 중.
**주의: '지난 방문 이후' 배너는 08-02에 배포됐으므로 이 7월 개선의 원인이 아니다.**
배너 효과는 08-02 이후 데이터로만 판단할 것. (원인 후보: 데이터 축적, 입소문, 토스 노출 변화)

## 사용 패턴 (건강한 신호)
- 요일별 평균 AU: 토 58.0 > 금 52.8 > 일 51.8 > 목 48.3 > 월 45.3 > 수 38.5 > 화 36.0
  → 주말·불금 집중. 외식 행동과 겹치므로 실제 필요 상황에서 쓰인다는 뜻(무작위 유입이면 안 나오는 패턴).
- 연령: **30~40대 61%**(40대 단독 28.5%), 20대 28.6%, 10대 1.8%, 60대+ 0.7%.
  → 가족 외식·직장인 축. 가격 민감층이라 행사 정보와 궁합이 맞음.
- 연령 합계 1,352 = AU 총합(08-02 포함)과 정확히 일치 → 연령표는 unique user가 아니라
  **user-days 기준**. 비율 해석은 유효하나 "순 사용자 수"로 읽으면 안 된다.

## 결정: 일주일 대기 (기능 추가 보류)
- 레버는 **첫 세션**이다. 하루 47명 중 43명이 첫 방문이라, 첫 방문자의 10초를 바꾸는 것이
  재방문율을 올리는 가장 빠른 길. 어제 만든 배너는 재방문자(8.5%)만 대상이라 대다수에게 무력.
- 그런데 **무엇을 고칠지 근거가 아직 없다.** 콘솔은 "왔다/안 왔다"만 알려주고 앱 안 행동은 모른다.
- 08-02에 넣은 계측이 그 공백을 채운다(모든 이벤트에 `visit_type` 동봉).
  **일주일 뒤 데이터를 보고 고친다. 그 전에 기능을 얹으면 무엇이 효과였는지 다시 알 수 없다.**

## 일주일 뒤 확인할 것 (순서대로)
1. **계측 신뢰성 먼저.** 내 `visit_type` 신규/재방문 비율이 콘솔 `new_au` 비율과 비슷한가.
   크게 어긋나면 **토스 웹뷰가 세션 간 localStorage를 비우는 것**이므로(`ff_last_visit` 의존),
   재방문 판정과 '지난 방문 이후' 배너 자체가 무력화된 상태다. 이 경우 저장 방식부터 재설계.
2. **첫 방문자 이탈 지점.** `visit_type: new`인 `app_open` 대비 후속 click이 하나도 없는 비율
   = 아무것도 안 하고 나간 사람. 이 값이 크면 첫 화면 자체가 문제.
3. **첫 방문자가 고르는 행동.** deal_detail / tab_map 중 무엇을 먼저 누르는가.
   지도를 먼저 누른다면 "내 주변"이 진짜 니즈이므로 위치 권한 플로우가 최우선 개선 대상.
4. **배너 CTR.** since_visit_banner impression 대비 click. 낮으면 문구·위치 문제, 높은데
   재방문이 안 늘면 배너가 아니라 돌아올 이유(콘텐츠) 자체의 문제.
5. **찜의 예측력.** deal_like를 한 사용자가 실제로 재방문하는가. 그렇다면 찜 유도가 레버.

---

# 2026-08-02 — 계측 도입 (출시 후 DAU 30~50 상황)

## 배경
- 앱은 이미 출시 상태이고 **일 방문자 30~50명이 꾸준히** 있음. 그런데 계측 코드가 **전무**했다.
- 이 규모에서 광고 수익은 월 수천 원대라 광고 조정은 손익에 거의 영향이 없다. 반면
  **"매일 오는 30~50명이 같은 사람인가, 매번 새 사람인가"**를 모르면 다음 할 일을 정할 수 없다.
  전자면 유입만 늘리면 되고, 후자면 유입을 늘려도 밑 빠진 독이다. → 계측을 최우선으로 결정.

## 왜 토스 Analytics인가 (새 의존성 추가 안 함)
- `@apps-in-toss/web-framework`가 `@apps-in-toss/web-analytics`를 재수출한다. 이미 있는 SDK다.
- `Analytics.screen / impression / click` 세 가지뿐이고, 토스 콘솔 지표로 바로 집계된다.
- GA4 등 외부 도구를 붙이지 않은 이유: 백엔드가 없고, 외부 스크립트는 토스 웹뷰에서
  차단 리스크가 있으며, 사용자 데이터를 제3자로 보내지 않는 편이 낫다.
- **동적 import로 격리했다.** ads.js는 module이라 정적 import가 실패하면 광고·공유·위치까지
  전부 죽는다. 계측은 실패해도 앱이 멀쩡해야 하므로 `import().catch(() => {})`로 분리하고
  `window.tossLog`는 로드 실패 시 조용히 무시한다.

## 무엇을 재는가 (8지점)
- `screen: app_open` — **핵심.** `visit_type`(new/returning), `days_since_last_visit`,
  `liked_count`, `since_last_visit_new`를 함께 보낸다. 이 하나로 재방문 여부와 주기가 나온다.
  - `ff_last_visit`(타임스탬프)를 새로 저장. 없으면 `days_since_last_visit: -1` = 첫 방문.
- `impression/click: since_visit_banner` — 어제 넣은 '지난 방문 이후' 배너가 실제로
  재방문에 기여하는지 CTR로 판정하려고 노출과 클릭을 짝지어 잰다.
  노출은 `sinceImpressionLogged`로 **방문당 1회**만 (updateAlertBanners가 필터 토글마다 돌기 때문).
- `click`: `deal_detail`, `deal_like`(찜=재방문 의사 최강 신호, 추가/해제 구분),
  `deal_share`(유일한 유기적 유입 경로), `tab_map`, `store_navigate`(전환 최종 지점).

## 검증 방법
- `window.tossLog`를 배열에 쌓는 스텁으로 갈아끼우고 `ff_last_visit`·`ff_seen`을 조작해
  첫 방문 / 3일 만의 재방문을 시뮬레이션 → `visit_type`·`days_since_last_visit`·
  `since_last_visit_new` 값이 의도대로 나오는 것을 확인.
- 길찾기는 `href`를 떼고 click해서 새 탭 이동 없이 onclick만 실행시켜 확인.

## 한계 (중요)
- **토스 앱 밖에서는 아무것도 집계되지 않는다.** 광고와 동일하게 SDK가 no-op이다.
  프리뷰에서 검증한 것은 "어떤 이벤트가 어떤 값으로 호출되는가"까지이고,
  실제 콘솔 적재 여부는 실기기에서 확인해야 한다.
- 지표가 쌓이려면 최소 1~2주는 기다려야 판단이 가능하다. 그 전에 기능을 더 얹으면
  무엇이 효과였는지 다시 알 수 없게 된다.

---

# 2026-08-02 — 4개 항목 작업 (KFC 표기 / 광고 튜닝 / 재방문 동기 / 파일 분리)

## 1. KFC 매장 "미지원"은 이미 해결된 상태였음
- 주석·워크플로우 스텝 이름에는 "KFC 아직 미지원"이 남아 있었지만, `kakao-stores.js`의
  BRANDS에 KFC·서브웨이가 이미 들어 있고 stores.json에 KFC 186건·서브웨이 411건이 실재함.
- **코드 변경 없이 낡은 주석만 정정.** 같은 이유로 index.html의 "버거킹/노브랜드버거는
  이벤트 크롤러가 없다"는 주석도 사실과 달라 수정(각각 17건·14건 수집 중).
- 교훈: 이 저장소는 주석이 구현보다 뒤처져 있는 구간이 있으니, 주석을 근거로 판단하지 말 것.

## 2. 광고 트리거에 전역 가드 추가
- 기존 엔진은 트리거별 `every`만 있고 **전역 상한이 없었다.** map(2회당1)·navigation(매번)·
  detail(4회당1)을 번갈아 밟으면 짧은 시간에 전면광고가 연달아 뜰 수 있는 구조.
- 트리거와 무관한 3중 가드를 추가: 시작 유예 45초 / 전역 쿨다운 90초 / 세션 상한 5회.
- `navigation: every 1 → 2`. 길찾기는 구매의사가 가장 높지만 매장을 비교하며 연타하는
  패턴이 잦아 매번 띄우면 이탈 위험이 더 큼.
- **카운터 증가 순서 주의**: `interstitialReady` 확인을 카운터 증가 *뒤*, 노출 카운트 증가
  *앞*에 둬서, 광고가 준비 안 된 상태를 세션 상한 소진으로 세지 않게 했다.

## 3. 재방문 동기 — "지난 방문 이후 새 행사"
- 문제: `deals.json.isNew`는 크롤러가 **전날 파일과 비교해** 매기는 값이라 "어제 대비 신규"다.
  3일 만에 들어온 사용자에게는 그동안 쌓인 행사가 전혀 신규로 보이지 않는다.
- 해결: 방문 시점의 행사 키 집합을 `localStorage['ff_seen']`에 저장하고, 다음 방문에
  없던 키를 개인화 신규로 계산 → 초록 배너 + 탭하면 그 행사만 필터.
- **의도적 설계 판단 3가지.**
  - 첫 방문(스냅샷 없음)은 전부 신규가 되어 의미가 없으므로 배너를 띄우지 않는다.
  - `ALL_DEALS.length === 0`(로드 실패)일 때는 스냅샷을 갱신하지 않는다. 갱신하면 다음
    방문에 전체가 신규로 잡히는 오탐이 난다.
  - localStorage 실패(프라이빗 모드·용량)는 삼켜서 "첫 방문"으로 취급. 앱은 계속 동작.
- 스냅샷은 로드 직후 즉시 갱신하므로 새로고침하면 배너가 사라진다. "지난 로드 이후"라는
  의미가 정확하고 구현이 가장 단순해서 이 동작을 선택함.

## 4. index.html 1547줄 → 3파일 분리
- `<style>` → `public/app.css`(144줄), 인라인 `<script>` → `public/app.js`(1168줄),
  index.html은 마크업 299줄만 남김.
- **`type="module"`을 쓰지 않았다.** 마크업의 `onclick="toggleNewOnly()"` 같은 인라인
  핸들러가 전역 함수를 찾아야 하는데, 모듈로 만들면 스코프가 갇혀 전부 깨진다.
  일반 `<script src>`는 기존 인라인 스크립트와 실행 시점·스코프가 동일해 무손실 이동.
- 따라오는 변경 2곳: `build-toss.js` 복사 목록, `sw.js` PRECACHE + 캐시 버전 v1→v2
  (버전을 올려야 기존 사용자의 구버전 캐시가 activate에서 삭제됨).

## 검증에서 배운 것
- 프리뷰에서 `switchTab('map')` 후 지도가 안 그려지는데, 원인은 **아래 Lottie 노트와 같은
  rAF 스로틀**이었다. switchTab은 rAF 2중첩 안에서 `initMapIfNeeded()`를 부른다.
  `initMapIfNeeded()`를 직접 호출하니 타일·마커 정상(매장 5,243건, 마커 7개).
  → 지도 관련 검증은 앞으로도 rAF를 우회해 직접 호출할 것.
- 클래스 검사에 `className.includes('hidden')`을 쓰면 **`overflow-hidden`에 오탐**한다.
  `classList.contains('hidden')`를 쓸 것.

---

# (이전) 3단계 히어로 애니메이션 컨텍스트 노트

## 결정 배경
- 사용자 요청: 3단계 Lottie 마이크로 애니메이션. NVIDIA 무료 AI는 Lottie 미생성 → 폐기.
- 사용자 선택: "히어로만 + 직접 생성" → 이후 "꼭 코인이어야 하나, 다른 대안?"

## Lottie 시도와 폐기 (중요)
- lottie-web(경량/전체 빌드 모두) 자체 호스팅 + 지연 로드 + 상세 헤더 마운트까지 구현했으나,
  **개발 프리뷰 브라우저가 requestAnimationFrame을 심하게 스로틀**하여 애니메이션이 진행되지 않음(currentFrame 정지).
- 그 결과 애니메이션 위치/스케일/투명도 Lottie가 레이어 transform을 `-999999`(초기값)로 두고 화면 밖으로 렌더 → **이 환경에서 어떤 Lottie 모션도 시각 검증 불가**.
- 정적 Lottie 도형은 정상 렌더됨. 즉 코드/JSON 버그가 아니라 프리뷰 환경 한계.
- **결론: 검증 불가한 Lottie 대신, 이 프리뷰에서 정상 렌더·검증되는 CSS/SVG로 히어로를 강화하기로 전환.** Lottie 관련 파일(lottie-light.min.js, public/lottie/) 및 코드 전량 제거.

## 최종 구현 (검증됨)
- 상세 시트 헤더(openDetail)에 카드 리스트의 카테고리별 SVG 씬(dealSceneHtml)을 재사용해 크게 표시.
  - 할인=금화 낙하, 배달=스쿠터, 1+1/경품=색종이, 신메뉴=별, 그 외=플로팅 브랜드 이모지.
- band-flow(흐르는 그라데이션) + band-shimmer(광택) 추가.
- 헤더는 .deal-card가 아니므로 뷰포트 일시정지 CSS(stage2)의 영향 없이 항상 재생.
- 프리뷰에서 금화 낙하 헤더 시각 확인 완료, 콘솔 에러 0.

## 참고
- 무료 Lottie: LottieFiles (Lottie Simple License, 상업용 허용, 출처표시 불요). AI 생성 오픈소스: diffusionstudio/lottie(MIT).
- 실제 기기에서 Lottie가 필요해지면, 프리뷰가 아닌 실기기에서 검증하는 조건으로 재도입 가능.
