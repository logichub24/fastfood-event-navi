# 컨텍스트 노트

---

# 2026-08-02 — 4개 항목 작업 (KFC 표기 / 광고 튜닝 / 재방문 동기 / 파일 분리)

## 1. KFC 매장 "미지원"은 이미 해결된 상태였음
- 주석·워크플로우 스텝 이름에는 "KFC 아직 미지원"이 남아 있었지만, `kakao-stores.js`의
  BRANDS에 KFC·서브웨이가 이미 들어 있고 stores.json에 KFC 186건·서브웨이 411건이 실재함.
- **코드 변경 없이 낡은 주석만 정정.** 같은 이유로 index.html의 "버거킹/노브랜드버거는
  이벤트 크롤러가 없다"는 주석도 사실과 달라 수정(각각 17건·14건 수집 중).
- 교훈: 이 저장소는 주석이 구현보다 뒤처져 있는 구간이 있으니, 주석을 근거로 판단하지 말 것.

## 2. 광고 트리거에 전역 가드 추가
- 기존 엔진은 트리거별 `every`만 있고 **전역 상한이 없었다.** map(2회당1)·navigation(매번)·
  detail(4회당1)을 번갈아 밟으면 짧은 시간에 전면광고가 연달아 뜰 수 있는 구조.
- 트리거와 무관한 3중 가드를 추가: 시작 유예 45초 / 전역 쿨다운 90초 / 세션 상한 5회.
- `navigation: every 1 → 2`. 길찾기는 구매의사가 가장 높지만 매장을 비교하며 연타하는
  패턴이 잦아 매번 띄우면 이탈 위험이 더 큼.
- **카운터 증가 순서 주의**: `interstitialReady` 확인을 카운터 증가 *뒤*, 노출 카운트 증가
  *앞*에 둬서, 광고가 준비 안 된 상태를 세션 상한 소진으로 세지 않게 했다.

## 3. 재방문 동기 — "지난 방문 이후 새 행사"
- 문제: `deals.json.isNew`는 크롤러가 **전날 파일과 비교해** 매기는 값이라 "어제 대비 신규"다.
  3일 만에 들어온 사용자에게는 그동안 쌓인 행사가 전혀 신규로 보이지 않는다.
- 해결: 방문 시점의 행사 키 집합을 `localStorage['ff_seen']`에 저장하고, 다음 방문에
  없던 키를 개인화 신규로 계산 → 초록 배너 + 탭하면 그 행사만 필터.
- **의도적 설계 판단 3가지.**
  - 첫 방문(스냅샷 없음)은 전부 신규가 되어 의미가 없으므로 배너를 띄우지 않는다.
  - `ALL_DEALS.length === 0`(로드 실패)일 때는 스냅샷을 갱신하지 않는다. 갱신하면 다음
    방문에 전체가 신규로 잡히는 오탐이 난다.
  - localStorage 실패(프라이빗 모드·용량)는 삼켜서 "첫 방문"으로 취급. 앱은 계속 동작.
- 스냅샷은 로드 직후 즉시 갱신하므로 새로고침하면 배너가 사라진다. "지난 로드 이후"라는
  의미가 정확하고 구현이 가장 단순해서 이 동작을 선택함.

## 4. index.html 1547줄 → 3파일 분리
- `<style>` → `public/app.css`(144줄), 인라인 `<script>` → `public/app.js`(1168줄),
  index.html은 마크업 299줄만 남김.
- **`type="module"`을 쓰지 않았다.** 마크업의 `onclick="toggleNewOnly()"` 같은 인라인
  핸들러가 전역 함수를 찾아야 하는데, 모듈로 만들면 스코프가 갇혀 전부 깨진다.
  일반 `<script src>`는 기존 인라인 스크립트와 실행 시점·스코프가 동일해 무손실 이동.
- 따라오는 변경 2곳: `build-toss.js` 복사 목록, `sw.js` PRECACHE + 캐시 버전 v1→v2
  (버전을 올려야 기존 사용자의 구버전 캐시가 activate에서 삭제됨).

## 검증에서 배운 것
- 프리뷰에서 `switchTab('map')` 후 지도가 안 그려지는데, 원인은 **아래 Lottie 노트와 같은
  rAF 스로틀**이었다. switchTab은 rAF 2중첩 안에서 `initMapIfNeeded()`를 부른다.
  `initMapIfNeeded()`를 직접 호출하니 타일·마커 정상(매장 5,243건, 마커 7개).
  → 지도 관련 검증은 앞으로도 rAF를 우회해 직접 호출할 것.
- 클래스 검사에 `className.includes('hidden')`을 쓰면 **`overflow-hidden`에 오탐**한다.
  `classList.contains('hidden')`를 쓸 것.

---

# (이전) 3단계 히어로 애니메이션 컨텍스트 노트

## 결정 배경
- 사용자 요청: 3단계 Lottie 마이크로 애니메이션. NVIDIA 무료 AI는 Lottie 미생성 → 폐기.
- 사용자 선택: "히어로만 + 직접 생성" → 이후 "꼭 코인이어야 하나, 다른 대안?"

## Lottie 시도와 폐기 (중요)
- lottie-web(경량/전체 빌드 모두) 자체 호스팅 + 지연 로드 + 상세 헤더 마운트까지 구현했으나,
  **개발 프리뷰 브라우저가 requestAnimationFrame을 심하게 스로틀**하여 애니메이션이 진행되지 않음(currentFrame 정지).
- 그 결과 애니메이션 위치/스케일/투명도 Lottie가 레이어 transform을 `-999999`(초기값)로 두고 화면 밖으로 렌더 → **이 환경에서 어떤 Lottie 모션도 시각 검증 불가**.
- 정적 Lottie 도형은 정상 렌더됨. 즉 코드/JSON 버그가 아니라 프리뷰 환경 한계.
- **결론: 검증 불가한 Lottie 대신, 이 프리뷰에서 정상 렌더·검증되는 CSS/SVG로 히어로를 강화하기로 전환.** Lottie 관련 파일(lottie-light.min.js, public/lottie/) 및 코드 전량 제거.

## 최종 구현 (검증됨)
- 상세 시트 헤더(openDetail)에 카드 리스트의 카테고리별 SVG 씬(dealSceneHtml)을 재사용해 크게 표시.
  - 할인=금화 낙하, 배달=스쿠터, 1+1/경품=색종이, 신메뉴=별, 그 외=플로팅 브랜드 이모지.
- band-flow(흐르는 그라데이션) + band-shimmer(광택) 추가.
- 헤더는 .deal-card가 아니므로 뷰포트 일시정지 CSS(stage2)의 영향 없이 항상 재생.
- 프리뷰에서 금화 낙하 헤더 시각 확인 완료, 콘솔 에러 0.

## 참고
- 무료 Lottie: LottieFiles (Lottie Simple License, 상업용 허용, 출처표시 불요). AI 생성 오픈소스: diffusionstudio/lottie(MIT).
- 실제 기기에서 Lottie가 필요해지면, 프리뷰가 아닌 실기기에서 검증하는 조건으로 재도입 가능.
