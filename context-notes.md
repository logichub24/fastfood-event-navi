# 컨텍스트 노트

---

# 2026-08-02 — 토스 콘솔 실측 기준선 (계측 시작 시점 스냅샷)

출처: 토스 콘솔 CSV 2종(AU 내역·연령 내역, 2026-07-02~08-02).
**08-02 행(au 30, new_au 0)은 집계 미완으로 보여 전 계산에서 제외.** 유효구간 07-05~08-01(28일).

## 핵심 숫자 — "매일 오는 사람이 같은 사람인가"에 대한 답
- 총 방문 1,322 user-days / 신규 1,210 / **재방문 112 = 8.5%**
- 일평균 AU 47.2, 그중 재방문은 **하루 4명**
- 즉 **밑 빠진 독이 맞다.** 한 달간 1,210명이 써보고 대부분 안 돌아왔다.

## 단, 추세는 명확한 개선 (4주 단조 증가)
| 주차 | AU | 재방문 | 비율 |
|---|---|---|---|
| 07-05~07-11 | 288 | 8 | 2.8% |
| 07-12~07-18 | 361 | 24 | 6.6% |
| 07-19~07-25 | 326 | 32 | 9.8% |
| 07-26~08-01 | 347 | 48 | **13.8%** |

총 방문은 평평한데 재방문만 5배 → 돌아오는 사람이 실제로 쌓이는 중.
**주의: '지난 방문 이후' 배너는 08-02에 배포됐으므로 이 7월 개선의 원인이 아니다.**
배너 효과는 08-02 이후 데이터로만 판단할 것. (원인 후보: 데이터 축적, 입소문, 토스 노출 변화)

## 사용 패턴 (건강한 신호)
- 요일별 평균 AU: 토 58.0 > 금 52.8 > 일 51.8 > 목 48.3 > 월 45.3 > 수 38.5 > 화 36.0
  → 주말·불금 집중. 외식 행동과 겹치므로 실제 필요 상황에서 쓰인다는 뜻(무작위 유입이면 안 나오는 패턴).
- 연령: **30~40대 61%**(40대 단독 28.5%), 20대 28.6%, 10대 1.8%, 60대+ 0.7%.
  → 가족 외식·직장인 축. 가격 민감층이라 행사 정보와 궁합이 맞음.
- 연령 합계 1,352 = AU 총합(08-02 포함)과 정확히 일치 → 연령표는 unique user가 아니라
  **user-days 기준**. 비율 해석은 유효하나 "순 사용자 수"로 읽으면 안 된다.

## 결정: 일주일 대기 (기능 추가 보류)
- 레버는 **첫 세션**이다. 하루 47명 중 43명이 첫 방문이라, 첫 방문자의 10초를 바꾸는 것이
  재방문율을 올리는 가장 빠른 길. 어제 만든 배너는 재방문자(8.5%)만 대상이라 대다수에게 무력.
- 그런데 **무엇을 고칠지 근거가 아직 없다.** 콘솔은 "왔다/안 왔다"만 알려주고 앱 안 행동은 모른다.
- 08-02에 넣은 계측이 그 공백을 채운다(모든 이벤트에 `visit_type` 동봉).
  **일주일 뒤 데이터를 보고 고친다. 그 전에 기능을 얹으면 무엇이 효과였는지 다시 알 수 없다.**

## 일주일 뒤 확인할 것 (순서대로)
1. **계측 신뢰성 먼저.** 내 `visit_type` 신규/재방문 비율이 콘솔 `new_au` 비율과 비슷한가.
   크게 어긋나면 **토스 웹뷰가 세션 간 localStorage를 비우는 것**이므로(`ff_last_visit` 의존),
   재방문 판정과 '지난 방문 이후' 배너 자체가 무력화된 상태다. 이 경우 저장 방식부터 재설계.
2. **첫 방문자 이탈 지점.** `visit_type: new`인 `app_open` 대비 후속 click이 하나도 없는 비율
   = 아무것도 안 하고 나간 사람. 이 값이 크면 첫 화면 자체가 문제.
3. **첫 방문자가 고르는 행동.** deal_detail / tab_map 중 무엇을 먼저 누르는가.
   지도를 먼저 누른다면 "내 주변"이 진짜 니즈이므로 위치 권한 플로우가 최우선 개선 대상.
4. **배너 CTR.** since_visit_banner impression 대비 click. 낮으면 문구·위치 문제, 높은데
   재방문이 안 늘면 배너가 아니라 돌아올 이유(콘텐츠) 자체의 문제.
5. **찜의 예측력.** deal_like를 한 사용자가 실제로 재방문하는가. 그렇다면 찜 유도가 레버.

---

# 2026-08-02 — 계측 도입 (출시 후 DAU 30~50 상황)

## 배경
- 앱은 이미 출시 상태이고 **일 방문자 30~50명이 꾸준히** 있음. 그런데 계측 코드가 **전무**했다.
- 이 규모에서 광고 수익은 월 수천 원대라 광고 조정은 손익에 거의 영향이 없다. 반면
  **"매일 오는 30~50명이 같은 사람인가, 매번 새 사람인가"**를 모르면 다음 할 일을 정할 수 없다.
  전자면 유입만 늘리면 되고, 후자면 유입을 늘려도 밑 빠진 독이다. → 계측을 최우선으로 결정.

## 왜 토스 Analytics인가 (새 의존성 추가 안 함)
- `@apps-in-toss/web-framework`가 `@apps-in-toss/web-analytics`를 재수출한다. 이미 있는 SDK다.
- `Analytics.screen / impression / click` 세 가지뿐이고, 토스 콘솔 지표로 바로 집계된다.
- GA4 등 외부 도구를 붙이지 않은 이유: 백엔드가 없고, 외부 스크립트는 토스 웹뷰에서
  차단 리스크가 있으며, 사용자 데이터를 제3자로 보내지 않는 편이 낫다.
- **동적 import로 격리했다.** ads.js는 module이라 정적 import가 실패하면 광고·공유·위치까지
  전부 죽는다. 계측은 실패해도 앱이 멀쩡해야 하므로 `import().catch(() => {})`로 분리하고
  `window.tossLog`는 로드 실패 시 조용히 무시한다.

## 무엇을 재는가 (8지점)
- `screen: app_open` — **핵심.** `visit_type`(new/returning), `days_since_last_visit`,
  `liked_count`, `since_last_visit_new`를 함께 보낸다. 이 하나로 재방문 여부와 주기가 나온다.
  - `ff_last_visit`(타임스탬프)를 새로 저장. 없으면 `days_since_last_visit: -1` = 첫 방문.
- `impression/click: since_visit_banner` — 어제 넣은 '지난 방문 이후' 배너가 실제로
  재방문에 기여하는지 CTR로 판정하려고 노출과 클릭을 짝지어 잰다.
  노출은 `sinceImpressionLogged`로 **방문당 1회**만 (updateAlertBanners가 필터 토글마다 돌기 때문).
- `click`: `deal_detail`, `deal_like`(찜=재방문 의사 최강 신호, 추가/해제 구분),
  `deal_share`(유일한 유기적 유입 경로), `tab_map`, `store_navigate`(전환 최종 지점).

## 검증 방법
- `window.tossLog`를 배열에 쌓는 스텁으로 갈아끼우고 `ff_last_visit`·`ff_seen`을 조작해
  첫 방문 / 3일 만의 재방문을 시뮬레이션 → `visit_type`·`days_since_last_visit`·
  `since_last_visit_new` 값이 의도대로 나오는 것을 확인.
- 길찾기는 `href`를 떼고 click해서 새 탭 이동 없이 onclick만 실행시켜 확인.

## 한계 (중요)
- **토스 앱 밖에서는 아무것도 집계되지 않는다.** 광고와 동일하게 SDK가 no-op이다.
  프리뷰에서 검증한 것은 "어떤 이벤트가 어떤 값으로 호출되는가"까지이고,
  실제 콘솔 적재 여부는 실기기에서 확인해야 한다.
- 지표가 쌓이려면 최소 1~2주는 기다려야 판단이 가능하다. 그 전에 기능을 더 얹으면
  무엇이 효과였는지 다시 알 수 없게 된다.

---

# 2026-08-02 — 4개 항목 작업 (KFC 표기 / 광고 튜닝 / 재방문 동기 / 파일 분리)

## 1. KFC 매장 "미지원"은 이미 해결된 상태였음
- 주석·워크플로우 스텝 이름에는 "KFC 아직 미지원"이 남아 있었지만, `kakao-stores.js`의
  BRANDS에 KFC·서브웨이가 이미 들어 있고 stores.json에 KFC 186건·서브웨이 411건이 실재함.
- **코드 변경 없이 낡은 주석만 정정.** 같은 이유로 index.html의 "버거킹/노브랜드버거는
  이벤트 크롤러가 없다"는 주석도 사실과 달라 수정(각각 17건·14건 수집 중).
- 교훈: 이 저장소는 주석이 구현보다 뒤처져 있는 구간이 있으니, 주석을 근거로 판단하지 말 것.

## 2. 광고 트리거에 전역 가드 추가
- 기존 엔진은 트리거별 `every`만 있고 **전역 상한이 없었다.** map(2회당1)·navigation(매번)·
  detail(4회당1)을 번갈아 밟으면 짧은 시간에 전면광고가 연달아 뜰 수 있는 구조.
- 트리거와 무관한 3중 가드를 추가: 시작 유예 45초 / 전역 쿨다운 90초 / 세션 상한 5회.
- `navigation: every 1 → 2`. 길찾기는 구매의사가 가장 높지만 매장을 비교하며 연타하는
  패턴이 잦아 매번 띄우면 이탈 위험이 더 큼.
- **카운터 증가 순서 주의**: `interstitialReady` 확인을 카운터 증가 *뒤*, 노출 카운트 증가
  *앞*에 둬서, 광고가 준비 안 된 상태를 세션 상한 소진으로 세지 않게 했다.

## 3. 재방문 동기 — "지난 방문 이후 새 행사"
- 문제: `deals.json.isNew`는 크롤러가 **전날 파일과 비교해** 매기는 값이라 "어제 대비 신규"다.
  3일 만에 들어온 사용자에게는 그동안 쌓인 행사가 전혀 신규로 보이지 않는다.
- 해결: 방문 시점의 행사 키 집합을 `localStorage['ff_seen']`에 저장하고, 다음 방문에
  없던 키를 개인화 신규로 계산 → 초록 배너 + 탭하면 그 행사만 필터.
- **의도적 설계 판단 3가지.**
  - 첫 방문(스냅샷 없음)은 전부 신규가 되어 의미가 없으므로 배너를 띄우지 않는다.
  - `ALL_DEALS.length === 0`(로드 실패)일 때는 스냅샷을 갱신하지 않는다. 갱신하면 다음
    방문에 전체가 신규로 잡히는 오탐이 난다.
  - localStorage 실패(프라이빗 모드·용량)는 삼켜서 "첫 방문"으로 취급. 앱은 계속 동작.
- 스냅샷은 로드 직후 즉시 갱신하므로 새로고침하면 배너가 사라진다. "지난 로드 이후"라는
  의미가 정확하고 구현이 가장 단순해서 이 동작을 선택함.

## 4. index.html 1547줄 → 3파일 분리
- `<style>` → `public/app.css`(144줄), 인라인 `<script>` → `public/app.js`(1168줄),
  index.html은 마크업 299줄만 남김.
- **`type="module"`을 쓰지 않았다.** 마크업의 `onclick="toggleNewOnly()"` 같은 인라인
  핸들러가 전역 함수를 찾아야 하는데, 모듈로 만들면 스코프가 갇혀 전부 깨진다.
  일반 `<script src>`는 기존 인라인 스크립트와 실행 시점·스코프가 동일해 무손실 이동.
- 따라오는 변경 2곳: `build-toss.js` 복사 목록, `sw.js` PRECACHE + 캐시 버전 v1→v2
  (버전을 올려야 기존 사용자의 구버전 캐시가 activate에서 삭제됨).

## 검증에서 배운 것
- 프리뷰에서 `switchTab('map')` 후 지도가 안 그려지는데, 원인은 **아래 Lottie 노트와 같은
  rAF 스로틀**이었다. switchTab은 rAF 2중첩 안에서 `initMapIfNeeded()`를 부른다.
  `initMapIfNeeded()`를 직접 호출하니 타일·마커 정상(매장 5,243건, 마커 7개).
  → 지도 관련 검증은 앞으로도 rAF를 우회해 직접 호출할 것.
- 클래스 검사에 `className.includes('hidden')`을 쓰면 **`overflow-hidden`에 오탐**한다.
  `classList.contains('hidden')`를 쓸 것.

---

# (이전) 3단계 히어로 애니메이션 컨텍스트 노트

## 결정 배경
- 사용자 요청: 3단계 Lottie 마이크로 애니메이션. NVIDIA 무료 AI는 Lottie 미생성 → 폐기.
- 사용자 선택: "히어로만 + 직접 생성" → 이후 "꼭 코인이어야 하나, 다른 대안?"

## Lottie 시도와 폐기 (중요)
- lottie-web(경량/전체 빌드 모두) 자체 호스팅 + 지연 로드 + 상세 헤더 마운트까지 구현했으나,
  **개발 프리뷰 브라우저가 requestAnimationFrame을 심하게 스로틀**하여 애니메이션이 진행되지 않음(currentFrame 정지).
- 그 결과 애니메이션 위치/스케일/투명도 Lottie가 레이어 transform을 `-999999`(초기값)로 두고 화면 밖으로 렌더 → **이 환경에서 어떤 Lottie 모션도 시각 검증 불가**.
- 정적 Lottie 도형은 정상 렌더됨. 즉 코드/JSON 버그가 아니라 프리뷰 환경 한계.
- **결론: 검증 불가한 Lottie 대신, 이 프리뷰에서 정상 렌더·검증되는 CSS/SVG로 히어로를 강화하기로 전환.** Lottie 관련 파일(lottie-light.min.js, public/lottie/) 및 코드 전량 제거.

## 최종 구현 (검증됨)
- 상세 시트 헤더(openDetail)에 카드 리스트의 카테고리별 SVG 씬(dealSceneHtml)을 재사용해 크게 표시.
  - 할인=금화 낙하, 배달=스쿠터, 1+1/경품=색종이, 신메뉴=별, 그 외=플로팅 브랜드 이모지.
- band-flow(흐르는 그라데이션) + band-shimmer(광택) 추가.
- 헤더는 .deal-card가 아니므로 뷰포트 일시정지 CSS(stage2)의 영향 없이 항상 재생.
- 프리뷰에서 금화 낙하 헤더 시각 확인 완료, 콘솔 에러 0.

## 참고
- 무료 Lottie: LottieFiles (Lottie Simple License, 상업용 허용, 출처표시 불요). AI 생성 오픈소스: diffusionstudio/lottie(MIT).
- 실제 기기에서 Lottie가 필요해지면, 프리뷰가 아닌 실기기에서 검증하는 조건으로 재도입 가능.
